#pragma once
#include <iostream>
#include <stack>
#include <sstream>
#include <math.h>
#include <string>
class Stack;
double exitString(std::string bufptr);
double mathOperations(std::string a);
void trigonometry(double value, int& count, std::string& exitString);
void createValue(std::string& value, char ch);
std::string valueProcessing(double value);

